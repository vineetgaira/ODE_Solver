This file contains the psuedocode that I have written for this program correct or incorrect.

--MENU

START:
    show_menu()
    INPUT:
        user choice
    IF choice == 1
        show method menu:
    IF choice == 2:
        show comparison b/w numerical methods
    IF choice == 3:
        show plot menu
    IF choice == 4:
        show method information
    IF choice == 5:
        show settings
    IF choice == 6:
        show supported operations
    IF choice == 7:
        Exit loop


How the file structure works : 

           main.py
              │
     ┌────────┴─────────┐
     │                  │
 menu.py        input_handler.py
                        │
                        ▼
                  validator.py
                        │
                        ▼
                    parser.py
                        │
                        ▼
                    solver.py
                        │
                        ▼
                   methods.py
                        │
                Solution History
              ┌─────────┬───────────┐
              │         │           │
         display.py  graph.py  exporter.py

=========
 METHODS :
=========
Euler
Modified Euler
Heun ------------- \
Midpoint------------> RK2
Ralston----------- /
RK4

    

Sovler:
    Which method to pick is the solvers job
    We have a METHODS_REGISTRY dict:
        METHODS_REGISTRY= {"euler": euler,
    "heun": heun,
    "midpoint": midpoint,
    "raltson": ralston,
    "rk4": rk4
}

    it stores the name and function.
    Now the things we need to execute a method individually:
        method_name(equation, x0, y0, h)
    
    so what the solver will look like:
        def solve_with_method(method_name, equation, x0, y0, target_x, h):
        method_fn = METHODS_REGISTRY[method_name]
        history = [(x0, y0)]
        n_steps = round((target_x - x0)/h)
        for i in range(n_steps):
            x0, y0 = method_fn(equation, x0, y0, h)
            history.append((x0, y0))
        return history


    